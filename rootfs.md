`/bin` `/sbin` `/dev` などのディレクトリ構造とファイル群の事。
 [[initramfs]] というものによって実ドライブがファイルシステムのルートにマウントされた後の初期状態のファイル構成。

- `ls` や `ps` などの [[ユーザー空間]] で行えるプログラムを指している。
- BusyBox というものを使用すると、基本的なコマンドを rootfs に含めることができる。