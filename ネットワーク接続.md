### カーネル側
ネットワークドライバーを有効にする設定を（確認）する。

`make menuconfig` で、Device Drivers → Network device support → Ethernet driver support から設定を行う。
QEMU用には「Intel(R) PRO/1000 Gigabit Ethernet support」にチェックをすれば良い。

※まだ挙動を確認できていない


